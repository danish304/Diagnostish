namespace Diagnostish.Infrastructure.Analyzers.HardwareInfoAnalyzers.Messages;

internal static class CommonMessages
{
    public static string CountOfTotal(int count, int total) => $"(у {count} из {total})";
}